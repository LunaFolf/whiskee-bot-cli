const CommandBase = require("../CommandBase.js");

module.exports = class DefaultClass extends CommandBase {
  constructor(message) {
    super(message);
    // if (STATEMENT) {
    //   this.response.success = true;
    // }
  }
};
